你是一个资深全栈架构师和代码迁移专家。请帮我把当前项目从 Python + Streamlit 架构，逐步迁移为 Spring Boot + React + Tauri 桌面应用。

当前项目主要功能包括：
1. 用户登录、多账户隔离、管理员后台
2. 学习登记
3. 知识点卡片
4. 错题记录
5. 复习计划
6. 每日 AI 复习
7. API Provider 管理
8. 本地加密 API Key 管理
9. API 余额查询
10. PPT / PDF 上传
11. PPT / PDF 页面解析
12. PPT 生成目录
13. PPT 逐页讲解
14. 同步阅读器
15. 页面插问
16. 从 PPT 阅读内容生成学习登记和知识卡片
17. 后台批量生成逐页讲解
18. 模型选择、最大 token、推理深度等 AI 调用配置

迁移目标：
把项目改造成一个现代桌面应用，架构如下：

frontend/
  React + TypeScript + Vite
  负责界面、路由、组件、状态管理、PPT 阅读器、AI 对话 UI

backend/
  Spring Boot 3 + Java 17 或 Java 21
  负责业务逻辑、数据库、认证、AI Provider、文件上传、PPT/PDF 解析、复习计划、知识卡片、后台任务

desktop/
  Tauri
  负责桌面端打包、窗口控制、本地文件访问、自动更新预留、跨平台发布

数据库：
优先使用 SQLite，方便桌面端本地部署。
后续预留切换 PostgreSQL / MySQL 的能力。

请严格遵守以下迁移原则：
1. 不要一次性重写全部功能。
2. 先分析现有 Python 项目结构和数据表。
3. 先输出迁移计划，再开始改代码。
4. 每一步迁移都要保证项目可运行。
5. 保留现有数据结构语义，尤其是 user_id 多账户隔离。
6. 所有新写入的数据必须带 user_id。
7. 不要破坏现有数据库文件，先提供迁移脚本和备份方案。
8. API Provider、PPT 讲解、知识卡片、每日复习是重点功能，要优先保证。
9. 所有外部模型接口都通过统一 AI Provider 抽象层调用。
10. 前端不要直接访问模型 API，必须调用 Spring Boot 后端。
11. Tauri 只做桌面壳，不要把复杂业务写进 Rust。
12. 代码要模块化，便于后续多人维护。

请先完成第 1 阶段：项目审计与迁移设计。

第 1 阶段任务：
1. 阅读当前仓库结构。
2. 找出 Streamlit 主入口、pages、services、db.py、models、prompts、components。
3. 总结当前功能模块。
4. 总结当前数据库表结构。
5. 总结当前 API Provider 设计。
6. 总结 PPT / PDF 处理流程。
7. 总结用户登录和 user_id 多账户隔离设计。
8. 找出当前项目里容易在迁移时出问题的地方。
9. 输出 Spring Boot + React + Tauri 的目标目录结构。
10. 输出分阶段迁移路线。

请按以下格式输出：

一、当前项目结构分析
二、当前核心功能模块
三、当前数据库表结构
四、当前 AI Provider 调用流程
五、当前 PPT / PDF 流程
六、当前登录和多账户隔离设计
七、迁移风险点
八、新架构目录设计
九、分阶段迁移计划
十、下一步要创建的文件清单

暂时不要直接大规模修改代码。